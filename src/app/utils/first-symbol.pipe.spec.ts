import { FirstSymbolPipe } from './first-symbol.pipe';

describe('FirstSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new FirstSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
